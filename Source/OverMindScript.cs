using Godot;
using Godot.Collections;
using System;

public class OverMindScript : Node2D {
    public int cash;


    public override void _Ready() {
        // File f = new File();
        // f.Open("res://Data/TestData.json", File.ModeFlags.Read);

        // GD.Print(f.GetAsText());

        // Dictionary j = JSON.Parse(f.GetAsText()).Result as Dictionary;
        // GD.Print(j["name"]);

        // if (d.FileExists("TestData.json")) {
        //     GD.Print("found it");


        // }
    }
}
